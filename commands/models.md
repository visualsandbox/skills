---
description: Browse the Visual Sandbox model catalogue with prices
argument-hint: [optional search words, or a modality like video]
---

# /models

Show what Visual Sandbox can run.

The filter is: **$ARGUMENTS**

Call `list_models`. Read the filter as a modality when it names one (image,
video, audio, 3d, vector), otherwise pass it as `query`. With no filter, list
everything.

Show a short table: model, what it is for, price per call, typical time. Put
the cheapest of each modality first. Do not paste the raw JSON.

End with one line on which model you would pick for the most common job in
that list, and why.
