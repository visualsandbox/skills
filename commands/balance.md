---
description: Show the Visual Sandbox credit left on this account
---

# /balance

Call `get_balance` and report the credit left, in dollars.

Then call `list_jobs` with `limit: 10` and say what the last ten runs cost in
total, so the user can see the rate they are spending at.

If the balance is under five dollars, say so plainly and give the top-up link.
